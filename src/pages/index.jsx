import { Todo } from "@/components/Todo";

export const Home = () => {
  return (
    <div className="min-h-screen flex justify-center items-center w-full m-auto">
      <Todo />
    </div>
  );
};

export default Home;
